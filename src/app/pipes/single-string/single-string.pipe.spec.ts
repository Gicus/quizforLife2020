import {SingleStringPipe} from './single-string.pipe';

describe('SingleStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SingleStringPipe();
    expect(pipe).toBeTruthy();
  });
});
