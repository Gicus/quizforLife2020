import { TestResponseModel } from './test-response-model';

describe('TestResponseModel', () => {
  it('should create an instance', () => {
    expect(new TestResponseModel()).toBeTruthy();
  });
});
